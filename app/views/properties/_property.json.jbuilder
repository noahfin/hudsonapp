json.extract! property, :id, :name, :street_num, :street_name, :city, :state, :zip, :zipcode_ext, :county, :latitude, :longitude, :address, :listing_broker, :available, :on_market, :expired_listing, :contact, :user_id, :contact_id, :marketing_id, :campaign_id, :todo_id, :created_at, :updated_at
json.url property_url(property, format: :json)
